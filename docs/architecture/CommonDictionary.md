# Common Dictionary

| Function | Purpose | Input | Return | Usage | Duplicate-prohibited names |
| --- | --- | --- | --- | --- | --- |
| manageButtonRenderer | Shared DataTable manage-column edit button renderer. It returns the standard small outline-primary edit button with the `seed-row-edit-btn` class and the Korean label `수정`. | object row, object options | string HTML | common/table/renderers/manage-button.js, ledger transaction/voucher list tables | manageButtonRenderer, manageButton |
| normalizeStatus | Normalizes voucher status values to uppercase underscore form, including hyphen/space variants and the legacy `CONFIRMED -> REVIEW_REQUESTED` alias. | mixed value, string fallback | string | ledger/shared/display-labels.js, voucher entry/review status rendering | normalizeStatus, normalizeVoucherStatus |
| statusBadge | Renders all voucher workflow statuses through the shared Bootstrap badge renderer using the shared `STATUS_LABELS` map; it also retains generic ledger processing-status badge support. | mixed status | string HTML | ledger/shared/display-labels.js, common/table/renderers/status-badge.js, voucher entry/review lists and detail cards | statusBadge, statusBadgeRenderer, renderJournalStatusBadge |

프로젝트 공용 함수 사전이다. 아래 함수명은 중복 생성 금지 대상이며, 신규 구현 전 기존 위치를 먼저 확인한다.

| 함수명 | 용도 | 입력값 | 반환값 | 사용 위치 | 중복 생성 금지 함수명 |
| --- | --- | --- | --- | --- | --- |
| requestPayload | HTTP 요청 본문 JSON 또는 form payload를 배열로 정규화한다. | 없음 | array | ImportController, TransactionController, BankTransactionReportController | requestPayload, parseRequestPayload, getRequestPayload |
| json | API 응답 payload와 HTTP status를 JSON으로 출력한다. | array/callable payload, int status | void | ImportController, EvidenceController, JournalRuleController, FileController, Funds Controllers | json, jsonResponse, respondJson |
| normalizeDataType | 자료유형 문자열을 ERP 표준 data type 코드로 정규화한다. | string type | string | ImportController, EvidenceGenerationService, EvidenceGenerationSplitService, SystemFieldService | normalizeDataType, normalizeImportType, normalizeSourceType |
| amountOrNull | 금액 입력값을 float 또는 null로 변환한다. | mixed value | ?float | ImportController, JournalRecommendationService | amountOrNull, parseAmountOrNull, nullableAmount |
| dateValue | 날짜 입력값을 저장 가능한 날짜 문자열로 정규화한다. | mixed value | string | ImportController | dateValue, normalizeDateValue |
| dateValueOrNull | 날짜 입력값을 저장 가능한 날짜 문자열 또는 null로 정규화한다. | mixed value | ?string | ImportController | dateValueOrNull, nullableDateValue |
| jsonEncodeForStorage | 저장용 JSON 문자열을 생성한다. | array payload | string | ImportController, EvidenceStatusService callback, EvidenceGenerationSaveService callback | jsonEncodeForStorage, encodeJsonForStorage, storageJson |
| tableExists | DB 테이블 존재 여부를 확인한다. | string table | bool | ImportController, VoucherController, TransactionController, Ledger Models, Ledger Services | tableExists, hasTable, checkTableExists |
| tableColumnExists | DB 컬럼 존재 여부를 확인한다. | string table, string column | bool | ImportController, VoucherController, TransactionController, Ledger Models | tableColumnExists, columnExists, hasColumn |
| isUuid | 문자열이 UUID 형식인지 확인한다. | string value | bool | ImportController, EvidenceGenerationService callback | isUuid, validUuid, isUuidValue |
| normalizeEvidenceMappedPayloadForResponse | 응답용 mapped payload를 화면 표시 규칙에 맞게 정규화한다. | array payload | array | ImportController, EvidenceGenerationService callback, EvidenceGenerationSaveService callback, EvidenceUploadService callback | normalizeEvidenceMappedPayloadForResponse, normalizeMappedPayloadForResponse |
| mappedPayloadForStorage | 저장용 mapped payload를 표준 key/value 구조로 정규화한다. | array row | array | ImportController, EvidenceGenerationSaveService callback, EvidenceGenerationSplitService callback | mappedPayloadForStorage, normalizePayloadForStorage |
| businessRefIdForStorage | payload의 거래처/프로젝트/직원/계좌/카드 참조 ID를 저장용으로 추출한다. | string refType, array payload | ?string | ImportController, EvidenceGenerationSaveService callback | businessRefIdForStorage, storageBusinessRefId |
| businessRefNameForStorage | payload의 거래처/프로젝트/직원/계좌/카드 참조명을 저장용으로 추출한다. | string refType, array payload | ?string | ImportController, EvidenceGenerationSaveService callback | businessRefNameForStorage, storageBusinessRefName |
| seedSourceKey | seed row 또는 payload에서 중복 방지용 source_key를 생성한다. | array row, string dataType | ?string | ImportController, EvidenceGenerationSaveService callback | seedSourceKey, buildSeedSourceKey |
| uploadVoucherStatus | 업로드/생성센터 저장 시 전표 상태를 산정한다. | string dataType, array payload, string processStatus | string | ImportController, EvidenceGenerationSaveService callback | uploadVoucherStatus, resolveVoucherStatus |
| bankVoucherValidationMessage | 은행 증빙 전표 생성 전 보정 필요 메시지를 산정한다. | array payload | ?string | ImportController, EvidenceGenerationSaveService callback | bankVoucherValidationMessage, bankVoucherErrorMessage |
| normalizeBankTransactionPayload | 은행 거래 payload 필드를 생성센터 표준 구조로 정규화한다. | array row | array | ImportController, EvidenceGenerationService callback, EvidenceGenerationSaveService callback, EvidenceUploadService callback | normalizeBankTransactionPayload, normalizeBankPayload |
| requiredFormatMissingMessages | 양식 필수 컬럼 누락 메시지를 생성한다. | array payload, array columns | array | ImportController, EvidenceGenerationSaveService callback | requiredFormatMissingMessages, requiredColumnMessages |
| businessProjectRuleMessages | 거래처/프로젝트 등 업무 참조 규칙 위반 메시지를 생성한다. | array payload | array | ImportController, EvidenceGenerationSaveService callback | businessProjectRuleMessages, businessReferenceRuleMessages |
| formatColumnsInOrder | 양식 컬럼 목록을 표시/저장 순서대로 정렬한다. | array columns | array | ImportController, EvidenceGenerationSaveService callback | formatColumnsInOrder, orderedFormatColumns |
| seedRowIdsFromPayload | 요청 payload에서 seed row id 목록을 추출한다. | array payload | array | ImportController, EvidenceGenerationSaveService callback, EvidenceStatusService flow, EvidenceTrashService flow | seedRowIdsFromPayload, idsFromSeedPayload |
| placeholdersForIds | SQL IN 조건용 placeholder 문자열과 parameter 배열을 생성한다. | array ids, string prefix | array | ImportController, EvidenceGenerationService callback, EvidenceGenerationSaveService callback, EvidenceStatusService callback, EvidenceTrashService callback | placeholdersForIds, buildInPlaceholders |
| isBlankValue | fill_blank 보정 정책에서 빈 값 여부를 판정한다. | mixed value | bool | ImportController, EvidenceGenerationSaveService callback | isBlankValue, blankValue, isEmptyPatchValue |

## 운영 규칙

- 공용 함수 추가 전 이 문서에 같은 역할이 있는지 확인한다.
- 같은 역할이 있으면 기존 함수를 callback 또는 Service 의존성으로 주입한다.
- 함수 시그니처를 변경할 때는 이 문서를 먼저 갱신한다.
- 사전 미갱신 상태의 공용 함수 추가는 작업 미완료로 본다.

| dateTimeValue | 날짜/시간 입력값을 저장 가능한 datetime 문자열 또는 null로 정규화한다. | mixed value | ?string | ImportController, EvidenceUploadService callback | dateTimeValue, normalizeDateTimeValue |
| number | 숫자/금액 입력값을 float로 정규화하고 null은 0으로 치환한다. | mixed value | float | ImportController, EvidenceBatchSaveService callback, EvidenceUploadService callback | number, numericValue, floatValue |
| cellValue | 셀 입력값을 문자열로 정규화한다. DateTime은 Y-m-d로 변환한다. | mixed value | string | ImportController, EvidenceUploadParserService callback | cellValue, normalizeCellValue |
| fieldLabel | 업로드/검증 필드 키를 사용자 표시 라벨로 변환하는 과도기 helper다. 장기 SSOT는 `SystemFieldService`가 생성한 DB physical-column meta와 persisted `columnDisplayName`이며, 화면 전용 하드코딩 라벨 맵은 제거 대상이다. | string field | string | ImportController, EvidenceUploadValidationService callback | fieldLabel, fieldDisplayName |
| normalizeCompanyNameForCompare | 회사명을 비교용 문자열로 정규화한다. 공백 제거와 소문자화를 적용한다. | string companyName | string | ImportController, EvidenceClientSyncService callback | normalizeCompanyNameForCompare, comparableCompanyName |

| mergeEvidenceBusinessInfoIntoPayload | 증빙 row의 business info를 payload에 보완 병합한다. | array evidenceRow, array payload | void | ImportController, EvidenceGenerationService callback, EvidenceTransactionCreateService callback, BundledVoucherService callback | mergeEvidenceBusinessInfoIntoPayload, mergeBusinessInfoPayload |
| partyFromRow | row와 prefix에서 사업자번호/회사명을 party 배열로 추출한다. | array row, string prefix, ?string fallbackPrefix | array | ImportController, EvidenceTransactionContextService callback | partyFromRow, buildPartyFromRow |
| isOwnCompanyParty | party가 자사 정보와 일치하는지 판정한다. | array party | bool | ImportController, EvidenceTransactionContextService callback | isOwnCompanyParty, matchOwnCompanyParty |
| ownCompanyProfile | 자사 사업자번호/회사명 비교 프로필을 반환한다. | 없음 | array | ImportController, EvidenceClientSyncService callback | ownCompanyProfile, companyProfileForCompare |
| normalizeBusinessNumber | 사업자번호 문자열을 숫자만 남기도록 정규화한다. | string businessNumber | string | ImportController, EvidenceRuleEngineService callback, EvidenceTransactionContextService callback, EvidenceUploadValidationService callback | normalizeBusinessNumber, businessNumberOnly |
| cleanCompanyName | 회사명 문자열을 비교/저장 전에 정리한다. | string companyName | string | ImportController, EvidenceRuleEngineService callback, EvidenceTransactionContextService callback, EvidenceUploadValidationService callback | cleanCompanyName, normalizeCompanyName |
| DataTableSettings | Shared DataTable settings controller for persisted table settings state such as visibleColumns, columnOrder, requiredColumns, columnDisplayName, and columnRequirementPolicy, plus separate view-state handling for columnWidths, searchFormExpanded, sortSettings, pageLength, and related layout refresh entry points. For DB-managed pages it regenerates missing defaults from `/api/settings/system/data-table-columns`, uses DB `ORDINAL_POSITION` for order, maps DB `IS_NULLABLE=NO` to `required` and `YES` to `none`, applies the shared system-column hidden-default rule through `settings_visible`, merges newly added DB columns into existing TABLE settings without resetting user order choices, gives newly added business columns their default visible state, prunes deleted columns, and writes the normalized result back to storage so `system_user_settings` stays synchronized with the latest schema. In DB-managed mode it persists TABLE concerns (`visibleColumns`, `columnOrder`, `columnDisplayName`, `columnRequirementPolicy`) and VIEW concerns (`columnWidths`, `pageLength`, `sortSettings`, `currentPage`, `searchFormExpanded`, `searchFormState`) to separate `setting_type` rows and keeps them independent in load/save/runtime handling. | tableSettings config, DataTable columns | object/module | common/table/data-table.js, common/datatable/dataTableSettings.js, common/table/search-form.js | DataTableSettings, tableSettingsManager |
| SystemUserSettingsStorage | Shared DB-only storage bridge for page settings keyed by `page_key` and `setting_type`. It resolves request context from explicit options plus storage-key conventions, prefers the server-provided `body[data-page-description]` value for `system_user_settings.description`, falls back to rendered breadcrumb text only when that server hint is absent, loads `exists` metadata, auto-bootstraps missing DB rows with generated defaults, supports synchronous delete for "restore defaults", debounced DB saves, and debug counters for per-`setting_type` load/save/delete verification. Legacy page-key migration is handled by deployment migration scripts, not by runtime alias lookup. The bridge stores and returns one `setting_type` payload at a time and never falls back to browser storage, legacy migration state, or combined runtime payloads. | string storageKey, object payload | object/module | common/user-settings/systemUserSettingsStorage.js, common/datatable/dataTableSettings.js, components/excel-manager/storage.js, common/grid/ag-grid-column-settings.js | SystemUserSettingsStorage, userSettingsStorageBridge |
| ExcelManagerSettingsCore | Shared Excel manager state controller for upload/download column settings. It builds `EXCEL_UPLOAD` and `EXCEL_DOWNLOAD` default states from the same DB-meta default entry rules used by DataTable settings, so restore defaults keeps column order, checkbox defaults, and required/optional star markers aligned with DB meta. Checkbox defaults come from DB/meta `settings_visible`; order comes from DB `ORDINAL_POSITION`; `EXCEL_UPLOAD` requirement defaults come from DB `IS_NULLABLE` (`NO -> required`, `YES -> none`) and persist `columnRequirementPolicy`, while `EXCEL_DOWNLOAD` persists only checkbox state and column order. When DB meta is available, default generation uses only DB physical columns and does not append JS registry-only supplemental columns. After default generation, each Excel domain saves and restores only its own `setting_type` payload through `SystemUserSettingsStorage`; browser storage, TABLE overlay, and cross-domain synchronization are not used. The standard initialization contract is `createExcelManagerSettingsCore({ domain, userSettingPageKey, formSelector, metaDomain })`, and screens are expected to pass canonical page keys and canonical metadata domains directly. Nonstandard table-settings bridging options are not used. | domain, userSettingPageKey, formSelector, metaDomain | object/module | components/excel-manager/core.js, components/excel-manager/ui.js, components/excel-manager/storage.js, components/excel-manager.js, common/datatable/dataTableSettings.js | ExcelManagerSettingsCore, excel-manager |
| DataTableColumnSettings | Shared modal UI for column visibility, column order, display name, and requirement policy management. | title, subtitle, entries, onSave | HTMLElement | common/datatable/dataTableColumnSettings.js | DataTableColumnSettings, columnSettingsModal |
| tableSettings | createDataTable option for attaching shared table settings behavior after DataTable initialization. | object | object | common/table/data-table.js, ledger/data pages | tableSettings, dataTableSettings |
| fixedLayout | createDataTable option that forces a non-scrollX table to fit its current container with a fixed table layout. Saved pixel widths remain persisted but are not forced onto the rendered cells while this mode is active, allowing split-layout tables to shrink and expand with their parent while keeping one aligned header/body table and ellipsis behavior. Runtime split panels use `setDataTableFixedLayout()` to enter this mode after clearing only rendered inline widths, then leave it to reapply the unchanged saved widths. | boolean | boolean | common/table/data-table.js | fixedLayout, dtFixedLayout, setDataTableFixedLayout |
| setDataTableFixedLayout | Shared runtime layout-mode switch for split-panel DataTables. Opening a panel clears only rendered inline widths and activates the existing fixed-container layout; closing it disables fixed layout and reapplies persisted `columnWidths` through `refreshDataTableLayout()` without changing the saved VIEW state. | DataTable table, boolean enabled | void | common/table/data-table.js, ledger/voucherReview.js | setDataTableFixedLayout, dataTableSplitLayout |
| widthScopeSelector | createDataTable option that overrides the shared width measurement scope with a closest ancestor selector so scrollX/sticky/header-body sync can align to a card or other container instead of the default table-box/content container scope. | string selector | string | common/table/data-table.js | widthScopeSelector, dataTableWidthScope |
| widthResizable | createDataTable column option that explicitly opts a column in or out of shared resize handles, including render-only or `data:null` columns; shared utility columns such as selection, reorder, sequence, status, and action now default to resizable unless a page sets `widthResizable: false`. | boolean | boolean | common/table/data-table.js | widthResizable, columnWidthResizable |
| selectionColumn | createDataTable option that customizes the auto-inserted checkbox column, including saved settings key, explicit width, and widthResizable behavior; the auto-inserted selection column is now included in the shared settings preparation stage so its saved width survives reload like other columns. | object | object | common/table/data-table.js | selectionColumn, dtSelectionColumn |
| showSelectionMoveButtons | createDataTable option that keeps the shared checkbox selection column enabled while suppressing the default selected-row up/down move buttons for review or read-focused screens that support multi-select actions but must not expose row-order management. | boolean | boolean | common/table/data-table.js, ledger/voucherReview.js | showSelectionMoveButtons, dtShowSelectionMoveButtons |
| ensureTableHeader | Shared DataTable header normalizer that rebuilds `thead > tr > th` from the active columns definition when the current header count mismatches or blank header cells are detected, so the DataTable columns definition remains the single header source of truth. | string tableSelector, array columns | void | common/table/data-table.js | ensureTableHeader, syncTableHeader |
| refreshDataTableLayout | Shared DataTable layout refresh entry point. It schedules sticky-offset recalculation plus `columns.adjust()` and configured-width reapply for real layout changes such as initial setup, sidebar toggle, column visibility change, responsive resize, modal close, and table-settings apply. When `draw:true` is requested it performs at most one draw on the first delayed pass only, then runs later passes as adjust-only stabilization. It must not be called from `draw.dt`, `xhr.dt`, or evidence child/processing-row post-processing. | DataTable table, object options | void | common/table/data-table.js, common/datatable/dataTableSettings.js, pages/layout/sidebar.js | refreshDataTableLayout, refreshLayout, refreshTableLayout |
| settingsKey | Stable DataTable column key fallback used by shared column visibility, order, sort, and width persistence when `data` is null or a render-only column is used. | string | string | common/datatable/dataTableSettings.js, common/table/data-table.js | settingsKey, columnSettingsKey |
| showColumnVisibility | createDataTable option that controls whether the legacy DataTables colvis button is rendered. Default is disabled so shared table settings UI becomes the primary column-control entry point. | boolean | boolean | common/table/data-table.js | showColumnVisibility, colvisEnabled |
| tableToolbarOrder | Shared createDataTable toolbar order standard. Default order is search, move-up, move-down, copy, delete, page buttons such as trash or excel, page-length, and table settings. Pages that do not use delete, trash, or excel hide those entries but keep the remaining order. | array/order rule | n/a | common/table/data-table.js, dashboard/settings pages | tableToolbarOrder, dataTableToolbarOrder |
| dt-content-stack | Shared management-page content stack class derived from the account-management standard flex column layout. It applies the same 14px spacing directly between an adjacent Search Form and DataTable, including when the search container is collapsed to zero height, so the toggle label does not overlap the table toolbar. | HTML container class | CSS layout | components/data-table.css, ledger evidence-policy view and reusable management views | dt-content-stack, dataTableContentStack |
| storageKey | Stable page-specific settings identifier used by shared settings modules to resolve the target `system_user_settings` row together with `page_key` and `setting_type`. It is no longer a browser-storage key. | string | string | common/datatable/dataTableSettings.js, components/excel-manager/storage.js, common/grid/ag-grid-column-settings.js | storageKey, settingsStorageKey |
| visibleColumns | Persisted configurable column visibility list in DataTable settings state. | array | array | common/datatable/dataTableSettings.js | visibleColumns, shownColumns |
| columnOrder | Persisted configurable column order list in DataTable settings state. | array | array | common/datatable/dataTableSettings.js | columnOrder, orderedColumns |
| columnDisplayName | Persisted shared column display-name map keyed by table-settings SSOT column key. It is used as the common display label source for table headers, modal labels, upload template/export headers, and upload/download flows when a page opts into shared DataTable settings. For ledger evidence, the key source must converge to `SystemFieldService + DB physical columns`, not `mapped_payload_json` field lists or page-local label maps. | object | object | common/datatable/dataTableSettings.js, common/datatable/dataTableColumnSettings.js, ledger/evidence-list.js | columnDisplayName, dataTableColumnDisplayName |
| columnRequirementPolicy | Persisted shared column requirement-policy map keyed by table-settings SSOT column key, with `none`, `optional`, and `required` values. It is the SSOT for modal required markers, upload validation, save validation, readiness/missing_fields generation, and related evidence required checks. For ledger evidence, default generation must come from DB physical-column metadata first and only keep `mapped_payload_json`-derived rules as transitional compatibility. | object | object | common/datatable/dataTableSettings.js, common/datatable/dataTableColumnSettings.js, ledger/evidence-list.js, app/Services/Ledger/EvidencePayloadNormalizeService.php | columnRequirementPolicy, dataTableColumnRequirementPolicy |
| ExcelTemplateFilenameHelper | Shared template-download filename normalizer that converts mixed legacy names such as `work-team-template.xlsx` and `bank_upload_template.xlsx` into snake_case `{domain}_template.xlsx` while leaving non-template exports untouched. | string filename, ?string fallbackBase | string | Core/Helpers/ExcelTemplateFilenameHelper.php, Ledger template download, System excel template download | ExcelTemplateFilenameHelper, normalizeTemplateFilename |
| ColumnPolicyRequestHelper | Shared request-policy parser for `column_display_name` and `column_requirement_policy`, including JSON decode, key normalization, per-column display-name resolution, and `none/optional/required` policy lookup across table settings, excel template/download, and upload flows. | mixed requestValue / array column | array/string | Core/Helpers/ColumnPolicyRequestHelper.php, System excel services, Ledger excel flows | ColumnPolicyRequestHelper, resolveColumnPolicyRequest |
| ExcelValueFormatterHelper | Shared Excel export formatter that writes identifier-like fields such as account numbers, business numbers, resident numbers, phone numbers, postal codes, card numbers, approval numbers, and code fields as explicit text cells so Excel does not apply scientific notation or strip leading zeros; it also normalizes selected bank-name code values to display names during export. | Worksheet sheet, array headers, array rows, ?string startCell, array columnMeta | void | Core/Helpers/ExcelValueFormatterHelper.php, System excel exports, Ledger evidence download | ExcelValueFormatterHelper, writeTable |
| columnWidths | Persisted DataTable view-state column width map stored by column key as widthPx, reapplied through shared header/body/colgroup width sync, visible-column-index header matching, and fixed-layout scrollX tables; when saved widths exist, the shared scroll/head width sync now preserves the saved total instead of stretching the table back out to the wrapper width on reload. In DB-managed mode it belongs to the VIEW `setting_type`, not TABLE. | object | object | common/table/data-table.js, common/datatable/dataTableSettings.js | columnWidths, savedColumnWidths |
| searchFormExpanded | Persisted SearchForm open or collapsed state stored in shared DataTable settings state by page-specific storageKey. | boolean | boolean | common/table/search-form.js, common/datatable/dataTableSettings.js | searchFormExpanded, savedSearchFormExpanded |
| sortSettings | Persisted DataTable sort state stored by column key, including multi-column order when available. | array | array | common/table/data-table.js, common/datatable/dataTableSettings.js | sortSettings, savedSortOrder |
| pageLength | Persisted DataTable page size selection restored after reload. | number | number | common/table/data-table.js, common/datatable/dataTableSettings.js | pageLength, savedPageLength |
| requiredColumns | Persisted required column key list that cannot be hidden in DataTable settings state. | array | array | common/datatable/dataTableSettings.js, ledger/data pages | requiredColumns, lockedColumns |
| PageKeyResolver | Shared server-side page key resolver that maps a permission row to canonical `system_page_registry.page_key` using `default_route_key`, `breadcrumb(description)`, breadcrumb aliases, and legacy prefix/exact fallback rules for PermissionRegistry sync, DB backfill, and future permission-page grouping. | string permissionKey, ?string description, ?string category | ?string pageKey | core/PageKeyResolver.php, core/PermissionRegistry.php | PageKeyResolver, resolvePageKey |
| actorDisplay | Actor field display resolver that returns `field_name`, raw `field`, then `(알 수 없음)` without parsing Actor tokens in UI code. | object row, string field | string | common/actor.js, common/table/data-table.js, common/grid/ag-grid-adapter.js | actorDisplay, actorNameDisplay |
| actorColumn | Shared DataTable actor column factory that renders Actor fields through `actorDisplay` and keeps raw values for non-display DataTable operations. | string field, string title, object options | object | common/actor.js, common/table/data-table.js | actorColumn, actorDataTableColumn |
| actorExcel | Shared Excel actor value resolver that applies the same display priority as `actorDisplay` for download flows. | object row, string field | string | common/actor.js, common/column-meta/adapters/excel.js | actorExcel, actorExcelValue |
| PickerSelect2.createAjax | Shared Select2 AJAX initializer used by `AdminPicker.select2Ajax`. If a search endpoint returns `success:false` or the AJAX request fails, it falls back to empty `results: []` so the UI shows a no-results state instead of surfacing a hard error. When the payload includes `sort_no`, it normalizes `results` / `data` / `items` arrays to `sort_no` ascending before page-level `processResults` and common none/add option wrapping so base-info and other picker lists stay aligned with screen order. | target, options | Select2 instance | common/picker/picker.select2.js, common/picker/admin_picker.js, ledger/shared/evidence-ref-picker.js, ledger/evidence-list/modal.js | PickerSelect2.createAjax, AdminPicker.select2Ajax |
| PickerSelect2.reloadOptions | Shared Select2 static-option reload helper used by `AdminPicker.reloadSelect2`. It preserves the common none/add options while reordering incoming items by `sort_no` ascending when present, allowing page-level manual option builders to keep ERP picker order aligned with the source screen SSOT. | target, items, valueKey, textKey, selectedValue, options | void | common/picker/picker.select2.js, common/picker/admin_picker.js, ledger/voucher/form.js | PickerSelect2.reloadOptions, AdminPicker.reloadSelect2 |
| keyboardMode | Shared AG Grid adapter option that switches keyboard behavior between the legacy web-table mode and the excel-style selection/editing split mode. `excel-selection` keeps single-click on selection only, leaves printable keys/F2/Ctrl+Enter to start editing, and lets page-level callbacks own Enter/Tab/Delete/Home/End row navigation. | string | string | common/grid/ag-grid-adapter.js, common/grid/ag-grid-keyboard.js, ledger/transaction.js | keyboardMode, agGridKeyboardMode |
| mergedDefaultColDef | Shared AG Grid adapter merge rule for `defaultColDef`. Adapter defaults and page-level `config.gridOptions.defaultColDef` must be merged first, and the merged result must be applied to the final `gridOptions` after any broad `...(config.gridOptions || {})` spread so common defaults such as `editable`, `resizable`, `sortable`, `suppressMovable`, and page-level callbacks like `suppressKeyboardEvent` are preserved together. The same overwrite-prevention rule applies to other shared adapter options such as `defaultColGroupDef`, `components`, `frameworkComponents`, `context`, `localeText`, `statusBar`, `sideBar`, `popupParent`, and `icons`. | adapter defaults, config.defaultColDef, config.gridOptions.defaultColDef | object/rule | common/grid/ag-grid-adapter.js | mergedDefaultColDef, defaultColDefMerge, agGridAdapterMerge |
| AgGridColumnSettings | Shared AG Grid column-width settings helper for DB-backed load, collect, apply, save, and debounced scheduleSave flows keyed by stable field/colId values. It persists width state through `SystemUserSettingsStorage` with per-grid page keys instead of browser storage. | storageKey, api, columnDefs, timerStore, options | object/module | common/grid/ag-grid-column-settings.js, ledger/transaction/storage.js | AgGridColumnSettings, agGridColumnSettings |
| metaDomain | createDataTable `tableSettings` option that binds a screen to the canonical DB physical-column metadata domain so the shared table settings UI can fetch SSOT columns from `/api/settings/system/data-table-columns` instead of treating page JS `columns[]` as the source of truth. Evidence domains must keep this in field/page meta policy, not in import/source type normalization services. | string | string | common/table/data-table.js, common/datatable/dataTableSettings.js, dashboard/settings base pages, ledger evidence pages, funds/bank-transactions | metaDomain, dataTableMetaDomain |
| metaColumns | Shared DataTable settings metadata payload normalized from DB physical columns, including `key`, `label`, `ordinal_position`, `required`, `settings_visible`, and `settings_order`, where `required` comes from DB `IS_NULLABLE` and `settings_visible` comes from the shared system-column default-visibility rule (`id`, audit columns, version columns hidden by default; `sort_no` visible). | array | array | common/datatable/dataTableSettings.js, SystemController API | metaColumns, dataTableMetaColumns |
| buildDataTableDefaultMetaEntries | Shared DB-meta default entry generator that converts normalized physical-column metadata into the baseline `key`, source-title, display-name, visible-default, requirement-policy, and ordinal-position payload used by table settings aligned consumers such as Excel Manager default restore. | object config, object options | array | common/datatable/dataTableSettings.js, components/excel-manager/core.js | buildDataTableDefaultMetaEntries, dataTableDefaultMetaEntries |
| buildMissingPhysicalColumns | Shared DataTable helper that appends hidden placeholder column definitions for DB physical columns absent from a page render `columns[]`, allowing table settings to preserve DB SSOT order and visibility state without forcing every page to render every physical field. | array columns, array metaColumns | array | common/datatable/dataTableSettings.js | buildMissingPhysicalColumns, appendPhysicalPlaceholderColumns |
| HtmlGridState | Shared HTML Grid state factory and lifecycle normalizer for grid capabilities, row lifecycle, cell lifecycle, and clonable initial state. | object config | object/module | common/html-grid/state.js | HtmlGridState, createInitialGridState |
| HtmlGridEventBus | Shared HTML Grid event bus with `on`, `off`, `emit`, `once`, `clear`, and listener counting for internal module coordination without direct DOM custom-event coupling. | none / eventName, handler, payload | object/module | common/html-grid/event-bus.js | HtmlGridEventBus, createGridEventBus |
| HtmlGridApi | Shared HTML Grid public API facade that exposes row, cell, state, capability, selection, render, validation, and destroy entry points while delegating real behavior to injected core handlers. `updateRow` accepts render-timing options such as `deferRowRender` so editor lifecycle-sensitive commits can update state before DOM replacement. | object core | object/module | common/html-grid/api.js | HtmlGridApi, createGridApi |
| HtmlGridSchema | Shared HTML Grid immutable runtime schema builder that validates required column keys, detects duplicate keys, merges default column properties, and verifies referenced editor, plugin, and formatter names against supplied registries. | array columns, object options | object/module | common/html-grid/schema.js | HtmlGridSchema, createRuntimeSchema |
| HtmlGridColumnManager | Shared HTML Grid column-state SSOT manager that owns order, visibility, pinned, width, and column meta state separately from row state and returns immutable snapshots through manager APIs such as `setOrder`, `setVisible`, `setWidth`, `setPinned`, and `setMeta`. | object config | object/module | common/html-grid/column-manager.js | HtmlGridColumnManager, createColumnManager |
| HtmlGridRenderer | Shared HTML Grid renderer orchestrator that converts grid state into HTML by delegating header, body, footer, and empty rendering and exposing partial render helpers such as `renderRow`, `renderCell`, `appendRow`, `removeRow`, `updateRow`, and `updateCell` without mutating state. | object config | object/module | common/html-grid/renderer.js | HtmlGridRenderer, createGridRenderer |
| HtmlGridHeaderRenderer | Shared HTML Grid header renderer that creates header cells, resize-handle slots, and optional sort UI placeholders from visible column state. | HTMLElement target, array columns, object options | HTMLElement/module | common/html-grid/header-renderer.js | HtmlGridHeaderRenderer, createHeaderRenderer |
| HtmlGridBodyRenderer | Shared HTML Grid body renderer that converts rows and visible columns into table rows and cells, delegates display formatting to formatter registry, and requests editor slots from editor registry without creating editors directly. | HTMLElement target, array rows, array columns, object options | HTMLElement/module | common/html-grid/body-renderer.js | HtmlGridBodyRenderer, createBodyRenderer |
| HtmlGridFooterRenderer | Shared HTML Grid footer renderer that displays per-column summary values and validation summary rows using footer state only. | HTMLElement target, array columns, object footerState, object options | HTMLElement/module | common/html-grid/footer-renderer.js | HtmlGridFooterRenderer, createFooterRenderer |
| HtmlGridEmptyRenderer | Shared HTML Grid empty-state renderer that displays loading, no-data, empty, and error states using class-based variants only. | HTMLElement target, object options | HTMLElement/module | common/html-grid/empty-renderer.js | HtmlGridEmptyRenderer, createEmptyRenderer |
| HtmlGridFormatterRegistry | Shared HTML Grid formatter registry that stores display-only formatter functions and resolves them by name; built-in formatters include text, number, currency, date, and percent. | object initialFormatters | object/module | common/html-grid/formatter.js | HtmlGridFormatterRegistry, createFormatterRegistry |
| HtmlGridEditorRegistry | Shared HTML Grid editor registry that registers editor factories by name, resolves them, creates editor instances from renderer context, and removes registrations without coupling renderer to concrete editor imports. | none | object/module | common/html-grid/editor-registry.js | HtmlGridEditorRegistry, createEditorRegistry |
| HtmlGridTextEditor | Shared HTML Grid plain-text input editor implementing the standard editor lifecycle and dirty-check contract. | object context | object/module | common/html-grid/editors/text-editor.js | HtmlGridTextEditor, createTextEditor |
| HtmlGridNumberEditor | Shared HTML Grid numeric input editor implementing formatted number input, decimal support, optional negative blocking, validation, and dirty-check without mutating grid state. | object context | object/module | common/html-grid/editors/number-editor.js | HtmlGridNumberEditor, createNumberEditor |
| HtmlGridDateEditor | Shared HTML Grid date input editor implementing normalized `YYYY-MM-DD` value handling and the standard editor lifecycle without datepicker coupling. | object context | object/module | common/html-grid/editors/date-editor.js | HtmlGridDateEditor, createDateEditor |
| HtmlGridSelectEditor | Shared HTML Grid select input editor implementing option rendering, value setting, validation, and dirty-check while treating options as editor input only. | object context | object/module | common/html-grid/editors/select-editor.js | HtmlGridSelectEditor, createSelectEditor |
| HtmlGridAddRowCommand | Shared HTML Grid command that adds a new row through context-provided row factories, updates selection, requests validation/footer recalculation, and emits row/selection events without touching DOM or renderer directly. | object payload | object/module | common/html-grid/commands/add-row-command.js | HtmlGridAddRowCommand, createAddRowCommand |
| HtmlGridInsertRowCommand | Shared HTML Grid command that inserts a row at a target index through context-provided row factories, updates selection, requests validation/footer recalculation, and emits row/selection events without touching DOM or renderer directly. | object payload | object/module | common/html-grid/commands/insert-row-command.js | HtmlGridInsertRowCommand, createInsertRowCommand |
| HtmlGridDeleteRowCommand | Shared HTML Grid command that enforces delete capability and row lifecycle policy, removes unsaved `created` rows physically, marks persisted rows as `deleted`, updates cell lifecycle/selection, and emits row/selection events without touching DOM or renderer directly. | object payload | object/module | common/html-grid/commands/delete-row-command.js | HtmlGridDeleteRowCommand, createDeleteRowCommand |
| HtmlGridMoveRowCommand | Shared HTML Grid command that reorders rows inside state, recalculates configured row sequence values, updates selection, requests validation/footer recalculation, and emits row/selection events without touching DOM or renderer directly. | object payload | object/module | common/html-grid/commands/move-row-command.js | HtmlGridMoveRowCommand, createMoveRowCommand |
| HtmlGridMoveCellCommand | Shared HTML Grid command that changes the active cell/selection and updates focused or selected cell lifecycle state without mutating values or touching DOM directly. | object payload | object/module | common/html-grid/commands/move-cell-command.js | HtmlGridMoveCellCommand, createMoveCellCommand |
| HtmlGridUpdateCellCommand | Shared HTML Grid command that is the primary cell-value mutation entry point. It updates row values, dirty fields, row lifecycle, cell lifecycle, selection, validation/footer state, and emits `cell:changed`, `selection:changed`, `validation:changed`, and `footer:changed` through the event bus without touching DOM or renderer directly. | object payload | object/module | common/html-grid/commands/update-cell-command.js | HtmlGridUpdateCellCommand, createUpdateCellCommand |
| HtmlGridUpdateRowCommand | Shared HTML Grid command that applies patch-based row updates only, updates dirty fields, row/cell lifecycle, selection, validation/footer state, and emits row/selection/validation/footer events without touching DOM or renderer directly. It forwards render-timing options such as `deferRowRender` through the row update event. | object payload | object/module | common/html-grid/commands/update-row-command.js | HtmlGridUpdateRowCommand, createUpdateRowCommand |
| HtmlGridSelection | Shared HTML Grid selection controller that manages active cell, active row, selected rows, range selection, and focus by calling command/API entry points only and never mutating renderer or DOM directly. | object config | object/module | common/html-grid/selection.js | HtmlGridSelection, createSelectionController |
| HtmlGridKeyboard | Shared HTML Grid keyboard controller that converts key events such as Tab, Enter, Arrow, Delete, Insert, Ctrl+D, Esc, F2, Ctrl+C, Ctrl+V, and Ctrl+A into command, API, or clipboard-hook calls without DOM traversal or direct state mutation. | object config | object/module | common/html-grid/keyboard.js | HtmlGridKeyboard, createKeyboardController |
| HtmlGridResize | Shared HTML Grid resize interaction controller that clamps widths by column min/max rules, calls `ColumnManager.setWidth()`, and emits `column:resized` on completion without touching renderer, bridge, or DOM persistence logic. | object config | object/module | common/html-grid/resize.js | HtmlGridResize, createResizeController |
| HtmlGridReorder | Shared HTML Grid drag-and-drop interaction controller that accepts drag-handle initiated row moves only and delegates actual order changes to `MoveRowCommand`. | object config | object/module | common/html-grid/reorder.js | HtmlGridReorder, createReorderController |
| HtmlGridValidator | Shared HTML Grid validation engine that runs built-in required/number/date checks, calls row and cell validation hooks, aggregates rowErrors/cellErrors/messages, and returns the normalized `validation` state without touching DB, API, renderer, or commands. | object config | object/module | common/html-grid/validator.js | HtmlGridValidator, createGridValidator |
| HtmlGridFooter | Shared HTML Grid footer calculation engine that computes summary values such as sum, count, and difference from state rows plus injected footer definitions or hooks and returns normalized footer state without mutating renderer or grid state. | object config | object/module | common/html-grid/footer.js | HtmlGridFooter, createGridFooter |
| HtmlGridSerializer | Shared HTML Grid serializer that converts state into versioned save payloads from rows, columns, and meta hooks only, without reading DOM or calling bridge logic directly. | object config | object/module | common/html-grid/serializer.js | HtmlGridSerializer, createGridSerializer |
| HtmlGridPluginRegistry | Shared HTML Grid plugin registry that registers external UI bridge plugins by name and manages plugin `init/mount/update/destroy` lifecycle per editor slot without letting plugins mutate grid state directly. | none | object/module | common/html-grid/plugin-registry.js | HtmlGridPluginRegistry, createPluginRegistry |
| HtmlGridSelect2Plugin | Shared HTML Grid plugin bridge for optional Select2 connection on select editors. It initializes only when an adapter or compatible jQuery Select2 runtime is available. | object context | object/module | common/html-grid/plugins/select2.js | HtmlGridSelect2Plugin, createSelect2Plugin |
| HtmlGridDatepickerPlugin | Shared HTML Grid plugin bridge for optional datepicker connection on date editors through externally supplied adapters. | object context | object/module | common/html-grid/plugins/datepicker.js | HtmlGridDatepickerPlugin, createDatepickerPlugin |
| HtmlGridNumberPlugin | Shared HTML Grid plugin bridge that marks numeric editor inputs with number-specific DOM hints such as decimal input mode without mutating grid state. | object context | object/module | common/html-grid/plugins/number.js | HtmlGridNumberPlugin, createNumberPlugin |
| HtmlGridCurrencyPlugin | Shared HTML Grid plugin bridge that marks currency editor inputs with currency-specific DOM hints while leaving actual state changes to editors and commands. | object context | object/module | common/html-grid/plugins/currency.js | HtmlGridCurrencyPlugin, createCurrencyPlugin |
| HtmlGridCodePickerPlugin | Shared HTML Grid plugin bridge for externally supplied code-picker widgets attached to editor elements only. | object context | object/module | common/html-grid/plugins/code-picker.js | HtmlGridCodePickerPlugin, createCodePickerPlugin |
| HtmlGridAccountPickerPlugin | Shared HTML Grid plugin bridge for externally supplied account-picker widgets attached to editor elements only. | object context | object/module | common/html-grid/plugins/account-picker.js | HtmlGridAccountPickerPlugin, createAccountPickerPlugin |
| HtmlGridCore | Shared HTML Grid Core assembler that wires schema, state, column manager, event bus, formatter/editor/plugin registries, renderer, validator, footer, serializer, command execution, and public API into a reusable grid instance. It also keeps table/empty-state visibility synchronized during full and partial renders, can defer row partial rendering for editor lifecycle-sensitive updates, and keeps validation changes from forcing active editor recreation. | object config | object/module | common/html-grid/index.js | HtmlGridCore, createHtmlGrid |
