<?php
declare(strict_types=1);define('PROJECT_ROOT',dirname(__DIR__));require PROJECT_ROOT.'/vendor/autoload.php';require_once PROJECT_ROOT.'/core/Storage.php';$db=Core\DbPdo::conn();$db->setAttribute(PDO::ATTR_EMULATE_PREPARES,true);$before=[];foreach(['user_employees','institution_employment_contracts','institution_regular_employment_incomes']as$t)$before[$t]=(int)$db->query("SELECT COUNT(*) FROM `$t`")->fetchColumn();$sql=(string)file_get_contents(PROJECT_ROOT.'/app/migrations/20260822_04_create_social_insurance_ssot.up.sql');$db->exec($sql);$after=[];foreach(array_keys($before)as$t)$after[$t]=(int)$db->query("SELECT COUNT(*) FROM `$t`")->fetchColumn();if($before!==$after)throw new RuntimeException('Migration 전후 기존 데이터 건수가 달라졌습니다.');$tables=$db->query("SELECT TABLE_NAME,COUNT(*) column_count FROM information_schema.COLUMNS WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME LIKE 'institution_social_insurance_%' GROUP BY TABLE_NAME ORDER BY TABLE_NAME")->fetchAll(PDO::FETCH_ASSOC);echo json_encode(['success'=>true,'before'=>$before,'after'=>$after,'tables'=>$tables],JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE).PHP_EOL;
